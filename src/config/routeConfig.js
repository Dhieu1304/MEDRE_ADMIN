const routeConfig = {
  home: "/",
  auth: "/auth",
  payment: "/payment",
  staff: "/staff",
  user: "/user",
  patient: "/patient",
  schedule: "/schedule",
  meeting: "/meeting",
  default: "/*"
};

export default routeConfig;
