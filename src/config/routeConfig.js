const routeConfig = {
  home: "/",
  auth: "/auth",
  payment: "/payment",
  staff: "/staff",
  user: "/user",
  patient: "/patient",
  schedule: "/schedule",
  setting: "/setting",
  meeting: "/meeting",
  default: "/*"
};

export default routeConfig;
