const routeConfig = {
  home: "/",
  auth: "/auth",
  payment: "/payment",
  staff: "/staff",
  user: "/user",
  schedule: "/schedule",
  meeting: "/meeting",
  booking: "/booking",
  default: "/*"
};

export default routeConfig;
